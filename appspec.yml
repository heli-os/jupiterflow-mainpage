version: 0.0
os: linux

files:
  - source: /
    destination: /home/jupiterflow/app/mainpage/
    overwrite: yes

permissions:
  - object: /home/jupiterflow/app/mainpage/
    owner: ec2-user
    group: ec2-user
    mode: 755
    type:
      - directory

hooks:
  AfterInstall:
    - location: deploy.sh
      timeout: 60